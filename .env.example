# Database
# Local Docker PostgreSQL (recommended for development)
# Start with: docker-compose -f docker-compose.local.yml up -d
# Connection pooling URL for edge/serverless functions (e.g., Supabase Pooler, Neon, PgBouncer)
DATABASE_URL="postgresql://daily_baker_user:local_dev_password@localhost:5434/daily_baker"
# Direct connection URL for migrations (no connection pooling)
DIRECT_DATABASE_URL="postgresql://daily_baker_user:local_dev_password@localhost:5434/daily_baker"

# Production/Hosted Database (alternative to local Docker)
# Use these if connecting to a hosted PostgreSQL instance
# DATABASE_URL="postgresql://user:password@host:port/daily_baker?pgbouncer=true"
# DIRECT_DATABASE_URL="postgresql://user:password@host:port/daily_baker"

# Clerk Authentication
# Get these from https://dashboard.clerk.com
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_SECRET_KEY=sk_test_xxxxx
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# AWS S3 Storage
# Get these from AWS IAM Console
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_S3_BUCKET_NAME=daily-baker-uploads

# AWS SES Email
# Configure in AWS SES Console
AWS_SES_REGION=us-east-1
AWS_SES_FROM_EMAIL=noreply@dailybaker.com

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Platform Admin Email (for initial seed)
PLATFORM_ADMIN_EMAIL=admin@example.com

# Sentry Error Tracking (optional)
# Get these from https://sentry.io
NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
SENTRY_ORG=your-organization-slug
SENTRY_PROJECT=daily-baker
# Auth token for uploading source maps (only needed for production builds)
SENTRY_AUTH_TOKEN=sntrys_xxxxx
